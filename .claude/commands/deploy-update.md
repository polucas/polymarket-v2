---
description: Push branch to VPS, pip install, restart systemd, verify. Usage: /deploy-update [branch]
---

Run the deploy-update skill with branch argument (if provided): $ARGUMENTS

Follow every step in `.claude/skills/deploy-update.md` exactly, including all confirmation gates before mutations.
