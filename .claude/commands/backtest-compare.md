---
description: Baseline vs override backtest comparison with Welch's t-test. Usage: /backtest-compare <start> <end> <KEY=VAL> [max_markets]
---

Run the backtest-compare skill with arguments: $ARGUMENTS

Follow every step in `.claude/skills/backtest-compare.md` exactly. Arguments: start_date (YYYY-MM-DD), end_date (YYYY-MM-DD), override (KEY=VALUE), optional max_markets (default 5000).
