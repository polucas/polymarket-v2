---
description: DB diagnostics — recipes: skip-breakdown | cash-reconcile | prescreen-failures | open-positions. Usage: /db-diagnostics <recipe> [hours]
---

Run the db-diagnostics skill with arguments: $ARGUMENTS

Follow every step in `.claude/skills/db-diagnostics.md` exactly. The first argument is the recipe name, second (optional) is hours window (default 48).
